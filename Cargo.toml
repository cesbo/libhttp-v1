[package]
name = "http"
version = "0.2.0"
authors = ["Cesbo Developers Team"]
edition = "2018"
publish = false

[dependencies]
base64 = "0.10"
openssl = "0.10"
libflate = "0.1"

[features]
static = ["openssl/vendored"]
